rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hackathon MVP: open access. Tighten before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
